import type {
  ITemplate,
  TemplateMeta,
  TemplateSection,
  TemplateStyles,
  TemplateCategory,
} from '../../types';

export type { ITemplate, TemplateMeta, TemplateSection, TemplateStyles, TemplateCategory };
